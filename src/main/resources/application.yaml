server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://172.31.249.212:5432/dev_freelance
    username: hamza_demo
    password: hamza123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html